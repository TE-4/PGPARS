@model IEnumerable<PGPARS.Models.Review>

<div class="main">
    <div class="container-opaque">
        <h2>Review Directory</h2>
        <a asp-action="AddReview" class="btn btn-success mb-3">New Review</a> <!-- Add Button -->
    <table class="table">
            <thead>
                <tr>
                    <th>Last Name</th>
                    <th>First Name</th>
                    <th>N Number</th>
                    <th>Status</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var review in Model)
                {
                    <tr>
                        <td>@review.LastName</td>
                        <td>@review.FirstName</td>
                        <td>@review.NNumber</td>
                        <td>@review.Status</td>
                        <td>
                            <a asp-action="Edit Review" asp-route-id="@review.ReviewId" class="btn-warning">Edit</a>
                            <form asp-action="DeleteReview" asp-route-id="@review.ReviewId" method="post" style="display:inline;">
                                <button type="submit" class="btn btn-danger"
                                        onclick="return confirm('Are you sure you want to delete this Review?');">
                                    Delete
                                </button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
<!--
    Do this 2nd
-->