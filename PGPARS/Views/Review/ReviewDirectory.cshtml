@model IEnumerable<PGPARS.Models.Review>
<body>
    <div class="main">
        <div class="container-opaque">
            <div class="text-center text-outline p-3">
                <h2>Review Directory</h2>
            </div>
        </div>
        <br />
        <div class="d-flex justify-content-between align-items-center">
            <!-- Add Button -->
            <a asp-action="AddReview" class="btn btn-success btn-lg" style="border: 2px solid black; min-height: 50px;">
                <i class="bi bi-upload"></i>&ensp;Add Review
            </a>
        </div>
        <div class="table-responsive shadow-sm rounded" style="padding-top: 10px;">
            <table class="table align-middle text-dark">
                <thead class="text-uppercase">
                    <tr>
                        <th>Name</th>
                        <th>N Number</th>
                        <th>Status</th>
                        <th>Review ID</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var review in Model)
                    {
                        <tr>
                            <td>@review.FullName</td>
                            <td>@review.NNumber</td>
                            <td>@review.Status</td>
                            <td>@review.ReviewNumber</td>
                            <!-- Actions column -->
                            <td>
                                <div class="d-flex justify-content-center gap-3">
                                    <a asp-action="EditReview" asp-route-id="@review.ReviewNumber">
                                        <button type="submit" class="btn btn-secondary btn-lg" style="color: white; border: 2px solid black;">
                                            <div class="bi bi-pencil">&ensp;Edit</div>
                                        </button>
                                    </a>
                                    <form asp-action="DeleteReview" asp-route-id="@review.ReviewNumber" method="post" style="d-inline-block">
                                        <button type="submit" class="btn btn-danger btn-lg" style="color: white; border: 2px solid black;"
                                                onclick="return confirm('Are you sure you want to delete this review?');">
                                            <div class="bi bi-trash">&ensp;Delete</div>
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</body>