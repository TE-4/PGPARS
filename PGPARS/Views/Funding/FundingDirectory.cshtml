@using Microsoft.IdentityModel.Tokens
@model IEnumerable<PGPARS.Models.Funding>

<div class="main">
    <div class="container-opaque">
        <!-- Title -->
        <div class="text-center p-3">
            Funding Directory
        </div>
    </div>
    <div>

        <!-- Search bar -->
        <form asp-action="FundingDirectory" method="get" class="mb-3">
            <div class="input-group">
                <input type="text" name="searchQuery" class="form-control" placeholder="Search by Name or Applicant..." value="@ViewData["SearchQuery"]" />
                <button type="submit" class="btn btn-primary">Search</button>
            </div>
        </form>

        <!-- Add Funding button -->
        <a asp-action="AddFunding" class="btn btn-success mb-3">Add New Funding</a>

        <!-- No results message -->
        @if (Model == null || !Model.Any())
        {
            <p class="text-muted">No funding entries found matching your search criteria.</p>
        }
        else
        {
            <!-- Table -->
            <table class="table">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Amount</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var fund in Model)
                    {
                        <tr>
                            <td>@fund.Cohort</td>
                            <td>@fund.Source</td>
                            <td>@fund.Amount?.ToString("C")</td>

                            <!-- Actions column -->
                            <td>
                                <a asp-action="EditFunding" asp-route-id="@fund.FundingID" class="btn btn-warning">Edit</a>
                                <form asp-action="DeleteFunding" asp-route-id="@fund.FundingID" method="post" style="display:inline;">
                                    <button type="submit" class="btn btn-danger"
                                            onclick="return confirm('Are you sure you want to delete this funding?');">
                                        Delete
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
    </div>
</div>
