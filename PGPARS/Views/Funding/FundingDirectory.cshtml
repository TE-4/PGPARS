@model IEnumerable<PGPARS.Models.Funding>

<div class="main">
    <div class="container-opaque">
        <div class="text-center p-3">
            Funding List
        </div>
    </div>
    <br />
    <div class="container-opaque">
        <table class="table">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Amount</th>
                    <th>Applicant</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var fund in Model)
                {
                    <tr>
                        <td>@fund.Name</td>
                        <td>@fund.Amount</td>
                        <td>
                            @if (fund.Applicant == null)
                            {
                                <a asp-action="AssignApplicant" asp-route-fundingId="@fund.Name" class="btn btn-primary">Assign</a>
                            }
                            else
                            {
                                @fund.Applicant
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
